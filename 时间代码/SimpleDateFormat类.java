package java代码.时间代码;

public class SimpleDateFormat类 {
/**
 * 构造器 描述
 *     SimpleDateFormat() 构造一个 SimpleDateFormat使用默认模式和日期格式符号默认 FORMAT区域设置。
 *     SimpleDateFormat   (String pattern) 构造一个 SimpleDateFormat使用给定的模式和默认的默认日期格式符号 FORMAT区域设置。
 *     SimpleDateFormat   (String pattern, DateFormatSymbols formatSymbols) 使用给定的模式和日期格式符号构造 SimpleDateFormat 。
 *     SimpleDateFormat   (String pattern, Locale locale) 使用给定模式和给定语言环境的默认日期格式符号构造 SimpleDateFormat 。
 *
 *     方法摘要
 *
 *     所有方法  实例方法 具体的方法   变量和类型 方法 描述
 *     void applyLocalizedPattern  (String pattern) 将给定的本地化模式字符串应用于此日期格式。
 *     void applyPattern​(String pattern) 将给定的模式字符串应用于此日期格式。
 *     Object clone() 创建此 SimpleDateFormat的副本。
 *     boolean equals​(Object obj) 比较给定对象与此 SimpleDateFormat是否相等。
 *     StringBuffer format​(Date date, StringBuffer toAppendTo, FieldPosition pos) 将给定的 Date为日期/时间字符串，并将结果附加到给定的 StringBuffer 。
 *     AttributedCharacterIterator formatToCharacterIterator​(Object obj) 格式化一个生成 AttributedCharacterIterator的对象。
 *     Date get2DigitYearStart() 返回100年期间的开始日期2位数年份被解释为在其中。
 *     DateFormatSymbols getDateFormatSymbols() 获取此日期格式的日期和时间格式符号的副本。
 *     int hashCode() 返回此 SimpleDateFormat对象的哈希码值。
 *     Date parse​(String text, ParsePosition pos) 解析字符串中的文本以生成 Date 。
 *     void set2DigitYearStart​(Date startDate) 设置100年期间2位数年份将被解释为在用户指定的日期开始。
 *     void setDateFormatSymbols​(DateFormatSymbols newFormatSymbols) 设置此日期格式的日期和时间格式符号。
 *     String toLocalizedPattern() 返回描述此日期格式的本地化模式字符串。
 *     String toPattern() 返回描述此日期格式的模式字符串。
 */

}
