minio:
  endpoint: http://45.207.208.76:9000
  access-key: UAIbQWcV0GgV363j
  secret-key: oVTjoebsuLebPsVMBqWpN3QIDrNLUFxY

server:
  port: 7658
spring:

  servlet:
    multipart:
      max-file-size: 1MB
      max-request-size: 10MB
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource  # 明确指定Druid数据源
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://45.207.195.166:3306/rkst?useSSL=false&serverTimezone=UTC&characterEncoding=utf8
    username: rkst
    password: rkst
    hikari:
      minimum-idle: 5
      maximum-pool-size: 10
      idle-timeout: 30000
      max-lifetime: 2000000
      connection-timeout: 30000
    druid:
      # 连接池的配置信息
      initial-size: 5  # 初始化连接数大小
      max-active: 30  # 最大连接数
      min-idle: 5   # 最小连接数

      # 配置获取连接等待超时的时间
      max-wait: 60000  #如果连接都被占用，最大等待时间1min=60000ms
      validation-query: SELECT 1 FROM DUAL  #通过从续表查询来监控是否连接数据库，语句执行能连接，语句不执行没连接
      # 配置一个连接在池中最小空闲的时间，单位是毫秒
      min-evictable-idle-time-millis: 300000
      test-while-idle: true
      # 开启SQL监控、防火墙监控、日志监控
      filters: stat,wall,slf4j
      # 配置DruidStatViewServlet
      stat-view-servlet:
        # 登录名
        login-username: admin
        # 登录密码
        login-password: admin
        url-pattern: /druid/*
        # IP白名单(没有配置或者为空，则允许所有访问)
        allow: 192.167.10.1,127.0.0.1,localhost  # 本机和设置的ip可以访问后台管理界面
        reset-enable: false # 不要重置
        # 启用控制台-后台管理界面，必须启用，否则访问界面会404
        enabled: true
  devtools:
    restart:
      enabled: true
      additional-paths: src/main/java
      exclude: WEB-INF/**
  mail:
    host: smtp.163.com
    username: z13039811650@163.com
    password: VZycv3AHQs7T8dVS
    # port: 587 #456
    default-encoding: UTF-8
    protocol: smtps
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
      # mail.smtp.starttls.required: true
      mail.smtp.ssl.enable: true
      mail.smtp.connection timeout: 5000  # 连接超时
      mail.smtp.timeout: 5000            # 读取超时
      mail.smtp.write timeout: 5000       # 写入超时
    port: 587

mybatis:
  type-aliases-package: com.example.boot.entity
  mapper-locations: classpath:mybatis/mapper/*.xml
